name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Welcome to Jaidev Studio! Thank you for opening this issue 🎉! The JD Team will review your work soon!'
        pr-message: 'Welcome to Jaidev Studio! Thank you for opening this pull request 🎉! The JD Team will review your work!'
